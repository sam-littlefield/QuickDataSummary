import { createMuiTheme } from '@material-ui/core/styles'

export default createMuiTheme({
	palette: {
		primary: {
			main: "#000000"
		},
		secondary: {
			main: "#FFCB05"
		}
	},
	typography: {
		useNextVariants: true,
		color: '#FDDD'
	},
	heading:{

	}
})
